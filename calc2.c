#include <cs50.h>
#include <stdio.h>

int main(void)
{

// prompt for x
    int x = get_int("x: ");

    // prompt for y
    int y = get_int("y: ");

    // divide x by y

    float z = (float) x / (float) y

    printf("%.2f\n", z);
}